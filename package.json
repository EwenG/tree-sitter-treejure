{
  "name": "tree-sitter-treejure",
  "version": "1.0.0",
  "main": "bindings/node",
  "devDependencies": {
    "tree-sitter-cli": "^0.26.5",
    "prebuildify": "^6.0.1"
  },
  "scripts": {
    "generate": "tree-sitter generate",
    "test": "tree-sitter test",
    "parse": "tree-sitter parse"
  }
}
